function TopoSystem() {
    // Create lists for nodePopulationList
    // Create lists for springPopulationList


    this.setup() {
        // setup the network topology
        // Loop through start lines
        // stPt = start point
        // endPt = end point
        // create nodeExists and set to false
        // loop through nodePopulationList
        // check if strt node is same as an end point of the line
        // if true - set as strtNode
        // if false - make a new one by calling a Particle object - nodePopulationList.append(Particle(strtPt))
        // in this case startNode = populationList[-1]

        // check if end node is same as an end point of the line
        // if true - set as endNode
        // if false - make a new one by calling a Node object - nodePopulationList.append(node(endPt))
        // in this case endNode = populationList[-1]

        // create a  spring from strt to endPt
        // springPopulationList.append(Spring(line, strtPrtcl, endPrtcl)
    }
  }
